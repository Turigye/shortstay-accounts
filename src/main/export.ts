import * as XLSX from "xlsx";import type Database from "better-sqlite3-multiple-ciphers";import type{BusinessSettings}from"../domain/types";import{buildExportWorkbook}from"../domain/export-workbook";import{createBookingRepository}from"./db/repositories/booking-repository";import{createPaymentRepository}from"./db/repositories/payment-repository";import{createExpenseRepository}from"./db/repositories/expense-repository";import{createCompensationRepository}from"./db/repositories/compensation-repository";import{createFinanceRepository}from"./db/repositories/finance-repository";import{createDashboardRepository}from"./db/repositories/dashboard-repository";
export function exportBusinessWorkbook(database:Database.Database,business:BusinessSettings,month:string,destination:string):void{const finance=createFinanceRepository(database,business.businessId);const book=buildExportWorkbook({business,period:month,scope:"Consolidated",exportedAt:new Date().toISOString(),dashboard:createDashboardRepository(database,business.businessId).getToday(`${month}-01`),bookings:createBookingRepository(database,business.businessId).listBookings(),payments:createPaymentRepository(database,business.businessId).listMovements(),expenses:createExpenseRepository(database,business.businessId).listExpenses(),staff:createCompensationRepository(database,business.businessId).getMonthlyReport(month as `${number}-${string}`),position:finance.getPosition(month),report:finance.getMonthlyReport(month)});XLSX.writeFile(book,destination,{compression:true});}
