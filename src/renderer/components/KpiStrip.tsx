const formatUgx=(value:number)=>`UGX ${new Intl.NumberFormat("en-UG",{notation:value>=1_000_000?"compact":"standard",maximumFractionDigits:1}).format(value)}`;
export function KpiStrip({occupancy,collected,outstanding,netPosition}:{occupancy:number;collected:number;outstanding:number;netPosition:number}){return <section className="kpi-strip" aria-label="Current month summary"><div><span>Occupancy</span><strong>{occupancy}%</strong></div><div><span>Collected</span><strong>{formatUgx(collected)}</strong></div><div><span>Outstanding</span><strong>{formatUgx(outstanding)}</strong></div><div><span>Net position</span><strong className={netPosition<0?"negative":undefined}>{formatUgx(netPosition)}</strong></div></section>}
